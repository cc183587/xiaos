# 2026-03-25 工作日志

## 员工列表列居中修复（02:08）
- 根因：`.header-id/.header-name/.header-op` 被查询记录表格的固定宽度（`flex:0 0 50px`）覆盖
- 将员工列表列名改为独立的 `emp-col-id/name/op`，彻底与查询记录解耦
- JS渲染同步更新为新类名

## 员工管理列表样式重写（02:05）
- 按参考代码重写 `.staff-card` / `.staff-header` / `.staff-item` 体系
- 表头改为浅蓝底色（`#E0E7FF`）+ 蓝色边框（`#C7D2FE`），3列等宽（`flex:1`）居中
- 行间距改为 `border-bottom: 1px dashed`，末行无底线
- `del-btn` 改为红色文字链接风格（无背景）

## 出货记录表头优化（02:03）
- 表头列名调整：批次→序号、数量→出货数量、时间→出货时间
- 表头背景改为 `#e8edf6`（蓝灰），加外边框，字体加粗加深更醒目
- 数据行 padding 与表头对齐

## 全文"工资"→"薪资"（02:00）
- 全局替换 index.html 中所有"工资"为"薪资"，共12处（含页面标题、JS注释等）
- 替换后0处残留，lint无报错

## 出入库卡片文案调整（01:58）
- 批次卡片"员工工资"→"员工薪资"（含今日总览卡片、KPI网格、Excel导出表头）
- 批次卡片"预估总利润"→"预估利润"
- 出货记录底部"累计利润"→"实际利润"（含Excel导出表头）
- 批次卡片产品名 `.batch-product` 改为黑色加粗（`color:#1a1a1a; font-weight:700`）

## 总览Tab卡片合并（01:53）
- 将本月/上月/累计三个独立 sv-section 卡片合并为一个带 Tab 切换的统一卡片（`.sv-tab-card`）
- 新增 `.sv-tabs` / `.sv-tab` / `.sv-tab-panel` 等 CSS 样式，三个 Tab 对应蓝/橙/紫三色高亮下划线
- 新增 `switchSvTab(tab)` JS 函数控制面板显示/隐藏与 Tab 激活状态
- 默认展示「本月」面板

## "工资统计"改名为"薪资明细"（01:49）
- 导航按钮、HTML注释、CSS注释、JS注释、Excel sheet名、导出文件名全部替换
- 共6处改动，均在 index.html

## 颜色 badge 功能（01:47）
- 批次卡片中产品颜色改为带底色的 badge 显示（圆角胶囊形）
- 颜色映射表（colorMap）：红=红底白字、黑=深色底白字、白=白底深灰字、粉=粉底深粉字、米白=黄底棕字
- 未知颜色兜底：浅灰底深灰字
- 修改位置：`adminLoadBatchList` 函数内，第 1423 行附近
